# Copy to .env.local for local dev (gitignored).
# In production, set this in the Vercel dashboard → Project → Settings → Environment Variables.
# This is Unsplash's public-ish Access Key, used by api/photo.ts server-side
# so it never appears in the client bundle.
UNSPLASH_ACCESS_KEY=
